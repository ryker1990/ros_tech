{
  "name": "wifi-control",
  "version": "0.1.0",
  "description": "Scan for, connect to, and disconnect from WiFi networks on Linux operating systems.",
  "main": "wifi-control.js",
  "license": "MIT",
  "dependencies": {
    "node-wifiscanner2": "^1.1.0",
    "sync-exec": "^0.6.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}